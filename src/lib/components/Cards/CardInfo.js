import styled from 'styled-components';

const CardInfo = styled.div`
  font-size: 1em;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  grid-row-gap: 6px;
`;

export default CardInfo;

export const CardInfoKey = styled.div`
  font-weight: bold;
`;

export const CardInfoProp = styled.div`

`;